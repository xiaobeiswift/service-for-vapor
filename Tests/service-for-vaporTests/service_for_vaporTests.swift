import XCTest
@testable import service_for_vapor

final class service_for_vaporTests: XCTestCase {
    func testExample() throws {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(service_for_vapor().text, "Hello, World!")
    }
}
